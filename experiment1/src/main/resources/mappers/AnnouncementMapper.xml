<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.myexperiment.mapper.AnnouncementMapper">
    
    <select id="findAll" resultType="com.myexperiment.entity.Announcement">
        SELECT id, title, content, teacher_name as teacherName, created_at as createdAt 
        FROM announcements 
        ORDER BY created_at DESC
    </select>

    <insert id="createAnnouncement" parameterType="com.myexperiment.entity.Announcement">
        INSERT INTO announcements (title, content, teacher_name)
        VALUES (#{title}, #{content}, #{teacherName})
    </insert>

</mapper>