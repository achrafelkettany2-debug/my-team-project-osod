<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.myexperiment.mapper.DiscussionMapper">
    
    <select id="findByCourseId" resultType="com.myexperiment.entity.Discussion">
        SELECT 
            id, 
            course_id as courseId, 
            username, 
            content, 
            created_at as createdAt 
        FROM discussions 
        WHERE course_id = #{courseId}
        ORDER BY created_at ASC
    </select>

    <insert id="createMessage" parameterType="com.myexperiment.entity.Discussion">
        INSERT INTO discussions (course_id, username, content)
        VALUES (#{courseId}, #{username}, #{content})
    </insert>

</mapper>